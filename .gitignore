# Build output
dist/
/webtraffik
/webtraffik.exe

# GeoLite2 database (auto-downloaded at runtime)
GeoLite2-City.mmdb

# SQLite event database (runtime data, not source)
events.db
events.db-wal
events.db-shm

# Local notes and planning (non-tracked)
.notes/

# eBPF compiled object files (generated by bpf2go; .go stubs are committed)
internal/ebpf/*.o
# vmlinux.h is generated per-machine from kernel BTF; not portable
internal/ebpf/bpf/programs/vmlinux.h
